schema-version = 2
release-version = "1.64.0"
project-vendor = "Bottlerocket"

[vendor.bottlerocket]
registry = "public.ecr.aws/bottlerocket"

# Local registry serving the core-kit built from the containerd-2.3 branch.
# TESTING ONLY - drop this commit before merge.
[vendor.local]
registry = "localhost:6126"

[sdk]
name = "bottlerocket-sdk"
version = "0.78.0"
vendor = "bottlerocket"

[[kit]]
name = "bottlerocket-kernel-kit"
version = "8.0.0"
vendor = "bottlerocket"

[[kit]]
name = "bottlerocket-core-kit"
version = "15.1.1"
vendor = "local"
